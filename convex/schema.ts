import { defineSchema } from "convex/server";

const schema = defineSchema({
  // Tables will be added by feature cards (P2-P4)
  // Schema is the system of record — never bypass with raw DB access
});

export default schema;
